<tr>
  <th><%= stored.name %></th>
  <th><%= stored.description %></th>
  <th><%= stored.created_at %></th>
  <th><%= stored.size %> байт</th>
  <th><i class="fa fa-window-close" aria-hidden="true" color="red"></i>
</th>
</tr>